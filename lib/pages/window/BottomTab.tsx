import { BottomTabBarProps } from "@react-navigation/bottom-tabs";
import { Text } from "react-native-ui-lib";

export default function BottomTab({}: BottomTabBarProps) {
	return (
		<Text>Waiting for developping</Text>
	)
}